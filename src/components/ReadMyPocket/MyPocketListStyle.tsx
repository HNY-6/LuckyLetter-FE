import styled from "styled-components";

const MyPocketListStyle = styled.ul`
  margin-top: 40px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  column-gap: 45px;
  row-gap: 40px;
`;

export default MyPocketListStyle;
